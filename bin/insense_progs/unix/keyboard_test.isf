type IKeyboard is interface(out string output)

component Keyboard presents IKeyboard
{
	constructor()
	{
	}

	behaviour
	{
		//getchar
		send buffer on output
		printString("Sent string on output:\n")
		printString(buffer)
	}
}

keyboard = new Keyboard()
